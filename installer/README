Please note that this entry should not be translated:

InstallUninstaller.85001a1b8498.destinationName=Uninstall
InstallUninstaller.85001a1b8498.destinationName=Uninstall

The above entry controls the name given to the "Uninstall.exe". Due to a bug in InstallAnywhere, if it is localised,
the uninstaller will not execute properly.

None of the "GUIComponentData.*" strings should be localized either.
They should be set to either "Selected" or "Not Selected", in English
Since these set the default values of radio buttons in the installer UI

Examples:
GUIComponentData.07ff15fd855d.defaultValue=Selected
GUIComponentData.07ff15fe855d.defaultValue=Not Selected
